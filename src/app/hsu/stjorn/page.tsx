import PlannerApp from "./PlannerApp";

export const dynamic = "force-dynamic";
export const metadata = { title: { absolute: "Vaktaskipulag — Vaktakerfi HSU" } };

export default function StjornPage() {
  return <PlannerApp />;
}
